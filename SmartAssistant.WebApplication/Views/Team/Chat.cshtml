@model IEnumerable<SmartAssistant.Shared.Models.Team.TeamModel>

<h2>Select a Team Chat</h2>

<ul>
    @foreach (var team in Model)
    {
        <li>
            <a href="@Url.Action("TeamChat", "Team", new { teamId = team.Id })">@team.TeamName</a>
        </li>
    }
</ul>
