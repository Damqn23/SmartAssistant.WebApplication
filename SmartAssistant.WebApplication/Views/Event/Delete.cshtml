@model SmartAssistant.Shared.Models.Event.EventModel

<div class="container mt-5">
    <h2 class="display-6 mb-4 text-danger">Delete Event</h2>

    <h3>Are you sure you want to delete this event?</h3>

    <div class="card border-danger mb-4">
        <div class="card-body">
            <h4 class="card-title" style="color: #dc3545;">@Model.EventTitle</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2" style="color: #007bff;">Event Title</dt>
                <dd class="col-sm-10">@Model.EventTitle</dd>
                <dt class="col-sm-2" style="color: #007bff;">Event Date</dt>
                <dd class="col-sm-10">@Model.EventDate.ToString("yyyy-MM-dd HH:mm")</dd>
            </dl>
        </div>
    </div>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary ms-2">Cancel</a>
    </form>
</div>